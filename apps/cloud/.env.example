# === PostgreSQL ===
POSTGRES_USER=bloguser
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=blogdb
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://bloguser:change_me_in_production@postgres:5432/blogdb

# === Redis ===
REDIS_URL=redis://redis:6379/0

# === 认证 ===
AUTH_SECRET_KEY=replace-with-a-very-long-random-string
AUTH_SESSION_EXPIRE_DAYS=30
AUTH_SESSION_COOKIE_NAME=session_id
AUTH_CSRF_COOKIE_NAME=csrf_token
AUTH_CSRF_HEADER_NAME=X-CSRF-Token
AUTH_COOKIE_PATH=/
AUTH_COOKIE_DOMAIN=.sakurakugu.top
# 原生 App 连接云端接口时属于跨站场景，需要 none 才能发送登录 Cookie
AUTH_COOKIE_SAMESITE=none
AUTH_COOKIE_SECURE=true

# === MinIO (兼容 S3) ===
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=blog-uploads
MINIO_USE_SSL=false
MINIO_PUBLIC_URL=https://api.sakurakugu.top/files

# === App ===
APP_ENV=production # development or production
APP_DEBUG=false
# 留空时按 APP_ENV 使用默认值：development 只保留原生壳来源，production 保留线上站点与原生壳来源
# 如果生产环境使用了自定义域名，或原生壳实际 Origin 与默认值不同，再显式填写 JSON 数组覆盖
CORS_ORIGINS=
SITE_URL=https://www.sakurakugu.top

# === Twikoo（前端构建时注入） ===
VITE_TWIKOO_ENV_ID=https://api.sakurakugu.top/twikoo/
VITE_TWIKOO_REGION=
TWIKOO_DATA_DIR=/app/twikoo-data
TWIKOO_CONTAINER_NAME=personal-system-twikoo-1
TWIKOO_CONTAINER_DATA_DIR=/app/data
DOCKER_SOCKET_PATH=/var/run/docker.sock

# === Admin ===
SUPER_ADMIN_USERNAME=superadmin
SUPER_ADMIN_EMAIL=superadmin@sakurakugu.top
SUPER_ADMIN_PASSWORD=change_me_super_admin

DEV_ADMIN_USERNAME=dev_admin
DEV_ADMIN_EMAIL=dev_admin@sakurakugu.top
DEV_ADMIN_PASSWORD=change_me_dev_admin

DEV_USER_USERNAME=dev_user
DEV_USER_EMAIL=dev_user@sakurakugu.top
DEV_USER_PASSWORD=change_me_dev_user
